name = "peel-api"
version = "0.1.0"
wasm = "peel-api.wasm"
wasm_sha256 = "b218b5a450f4a7963253c65ae961f822f7934dc7fd0bb194b41efd4eccad78ab"

provides = []
consumes = ["orchestrator.dispatch"]
depends_on = ["lua-orchestrator", "edge-admission"]
capabilities = ["transport.http.inbound", "entropy.read"]

[config]
api_addr = ":8080"
service_token = ""
admission_service_token = ""
join_lease_issuer_url = ""
join_hostname = ""
join_port = 5521

[config_env]
service_token = "SERVICE_TOKEN"
admission_service_token = "PEEL_ADMISSION_SERVICE_TOKEN"
join_lease_issuer_url = "JOIN_LEASE_ISSUER_URL"
join_hostname = "JOIN_HOSTNAME"
